// Code generated by 'gofp'. DO NOT EDIT.
package employer
import _ "reflect" 
import "sort" 
import "sync"
import "github.com/logic-building/functional-go/fp" 
import "github.com/logic-building/functional-go/internal/employee" 

func Map(f func(Employer) Employer, list []Employer) []Employer {
	if f == nil {
		return []Employer{}
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

func MapPtr(f func(*Employer) *Employer, list []*Employer) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapPtrErr takes 2 arguments:
//  1. A function input argument: *Employer and return types (*Employer, error)
//  2. A list of type []*Employer
//
// Returns:
// 	([]*Employer, error)
func MapPtrErr(f func(*Employer) (*Employer, error), list []*Employer) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		var err error
		newList[i], err = f(v)
		if err != nil {
			return nil, err
		}
	}
	return newList, nil
}

// MapErr takes 2 arguments:
//  1. A function input argument: Employer and return types (Employer, error)
//  2. A list of type []Employer
//
// Returns:
// 	([]Employer, error)
func MapErr(f func(Employer) (Employer, error), list []Employer) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		var err error
		newList[i], err = f(v)
		if err != nil {
			return nil, err
		}
	}
	return newList, nil
}

func Filter(f func(Employer) bool, list []Employer) []Employer {
	if f == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

func FilterPtr(f func(*Employer) bool, list []*Employer) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	var newList []*Employer
	for _, v := range list {
		if f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

// FilterPtrErr takes two arguments
//  1. Function: takes 1 argument of type Employer and returns (bool, error)
//  2. slice of type []*Employer
//
// Returns:
//  new filtered list and error
func FilterPtrErr(f func(*Employer) (bool, error), list []*Employer) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

// FilterErr takes two arguments
//  1. Function: takes 1 argument of type Employer and returns (bool, error)
//  2. slice of type []Employer
//
// Returns:
//  new filtered list and error
func FilterErr(f func(Employer) (bool, error), list []Employer) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

func Remove(f func(Employer) bool, list []Employer) []Employer {
	if f == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if !f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

func RemovePtr(f func(*Employer) bool, list []*Employer) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	var newList []*Employer
	for _, v := range list {
		if !f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

// RemovePtrErr removes the items from the given list based on supplied function and returns new list
//
// Takes 2 inputs:
//	1. Function: input type *Employer and return types(bool, error)
//	2. List of type: []*Employer
//
// Returns:
//	New list and error: ([]*Employer, error)
//	Empty list if both of arguments are nil or either one is nil.
func RemovePtrErr(f func(*Employer) (bool, error), list []*Employer) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

// RemoveErr removes the items from the given list based on supplied function and returns new list
//
// Takes 2 inputs:
//	1. Function: input type Employer and return types(bool, error)
//	2. List of type: []Employer
//
// Returns:
//	New list and error: ([]Employer, error)
//	Empty list if both of arguments are nil or either one is nil.
func RemoveErr(f func(Employer) (bool, error), list []Employer) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

func Some(f func(Employer) bool, list []Employer) bool {
	if f == nil {
		return false
	}
	for _, v := range list {
		if f(v) {
			return true
		}
	}
	return false
}

func SomePtr(f func(*Employer) bool, list []*Employer) bool {
	if f == nil {
		return false
	}
	for _, v := range list {
		if f(v) {
			return true
		}
	}
	return false
}

// SomePtrErr finds item in the list based on supplied function.
//
// Takes 2 input:
//	1. Function
//	2. List
//
// Returns:
//	(bool,err).
//	True if condition satisfies, else false
func SomePtrErr(f func(*Employer) (bool, error), list []*Employer) (bool, error) {
	if f == nil {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if r {
			return true, nil
		}
	}
	return false, nil
}

// SomeErr finds item in the list based on supplied function.
//
// Takes 2 input:
//	1. Function
//	2. List
//
// Returns:
//	(bool,err).
//	True if condition satisfies, else false
func SomeErr(f func(Employer) (bool, error), list []Employer) (bool, error) {
	if f == nil {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if r {
			return true, nil
		}
	}
	return false, nil
}

func Every(f func(Employer) bool, list []Employer) bool {
	if f == nil || len(list) == 0 {
		return false
	}
	for _, v := range list {
		if !f(v) {
			return false
		}
	}
	return true
}

func EveryPtr(f func(*Employer) bool, list []*Employer) bool {
	if f == nil || len(list) == 0 {
		return false
	}
	for _, v := range list {
		if !f(v) {
			return false
		}
	}
	return true
}

// EveryPtrErr returns true if supplied function returns logical true for every item in the list
func EveryPtrErr(f func(*Employer) (bool, error), list []*Employer) (bool, error) {
	if f == nil || len(list) == 0 {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if !r {
			return false, nil
		}
	}
	return true, nil
}

// EveryErr returns true if supplied function returns logical true for every item in the list
func EveryErr(f func(Employer) (bool, error), list []Employer) (bool, error) {
	if f == nil || len(list) == 0 {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if !r {
			return false, nil
		}
	}
	return true, nil
}

func DropWhile(f func(Employer) bool, list []Employer) []Employer {
	if f == nil {
		return []Employer{}
	}
	var newList []Employer
	for i, v := range list {
		if !f(v) {
			listLen := len(list)
			newList = make([]Employer, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList
		}
	}
	return newList
}

func DropWhilePtr(f func(*Employer) bool, list []*Employer) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	var newList []*Employer
	for i, v := range list {
		if !f(v) {
			listLen := len(list)
			newList = make([]*Employer, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList
		}
	}
	return newList
}

// DropWhilePtrErr drops the items from the list as long as condition satisfies.
//
// Takes two inputs
//	1. Function: takes one input and returns (boolean, error)
//	2. list
//
// Returns:
// 	New List, error
//  Empty list if either one of arguments or both of them are nil
func DropWhilePtrErr(f func(*Employer) (bool, error), list []*Employer) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			listLen := len(list)
			newList = make([]*Employer, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList, nil
		}
	}
	return newList, nil
}

// DropWhileErr drops the items from the list as long as condition satisfies.
//
// Takes two inputs
//	1. Function: takes one input and returns (boolean, error)
//	2. list
//
// Returns:
// 	New List, error
//  Empty list if either one of arguments or both of them are nil
func DropWhileErr(f func(Employer) (bool, error), list []Employer) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			listLen := len(list)
			newList = make([]Employer, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList, nil
		}
	}
	return newList, nil
}

func TakeWhile(f func(Employer) bool, list []Employer) []Employer {
	if f == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if !f(v) {
			return newList
		}
		newList = append(newList, v)
	}
	return newList
}

func TakeWhilePtr(f func(*Employer) bool, list []*Employer) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	var newList []*Employer
	for _, v := range list {
		if !f(v) {
			return newList
		}
		newList = append(newList, v)
	}
	return newList
}

// TakeWhilePtrErr returns new list based on condition in the supplied function. It returns new list once condition fails.
//
// Takes 2 inputs:
//	1. Function - 1 input of type *Employer and returns ([]*Employer, error)
//	2. List
//
// Returns:
//	New List, error.
//	Empty list if all the parameters are nil or either of one parameter is nil
func TakeWhilePtrErr(f func(*Employer) (bool, error), list []*Employer) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			return newList, nil
		}
		newList = append(newList, v)
	}
	return newList, nil
}

// TakeWhileErr returns new list based on condition in the supplied function. It returns new list once condition fails.
//
// Takes 2 inputs:
//	1. Function - 1 input of type Employer and returns ([]Employer, error)
//	2. List
//
// Returns:
//	New List, error.
//	Empty list if all the parameters are nil or either of one parameter is nil
func TakeWhileErr(f func(Employer) (bool, error), list []Employer) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			return newList, nil
		}
		newList = append(newList, v)
	}
	return newList, nil
}

// PMap applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMap(f func(Employer) Employer, list []Employer, optional ...fp.Optional) []Employer {
	if f == nil {
		return []Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapNoOrder(f, list, worker)
		}
	}

	return pMapPreserveOrder(f, list, worker)
}

func pMapPreserveOrder(f func(Employer) Employer, list []Employer, worker int) []Employer {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapNoOrder(f func(Employer) Employer, list []Employer, worker int) []Employer {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapPtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapPtr(f func(*Employer) *Employer, list []*Employer, optional ...fp.Optional) []*Employer {
	if f == nil {
		return []*Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapPtrNoOrder(f, list, worker)
		}
	}

	return pMapPtrPreserveOrder(f, list, worker)
}

func pMapPtrPreserveOrder(f func(*Employer) *Employer, list []*Employer, worker int) []*Employer {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapPtrNoOrder(f func(*Employer) *Employer, list []*Employer, worker int) []*Employer {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapPtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapPtrErr(f func(*Employer) (*Employer, error), list []*Employer, optional ...fp.Optional) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapPtrErrNoOrder(f, list, worker)
		}
	}

	return pMapPtrErrPreserveOrder(f, list, worker)
}

func pMapPtrErrPreserveOrder(f func(*Employer) (*Employer, error), list []*Employer, worker int) ([]*Employer, error) {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapPtrErrNoOrder(f func(*Employer) (*Employer, error), list []*Employer, worker int) ([]*Employer, error) {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	return newList, nil
}

// PMapErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapErr(f func(Employer) (Employer, error), list []Employer, optional ...fp.Optional) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapErrNoOrder(f, list, worker)
		}
	}

	return pMapErrPreserveOrder(f, list, worker)
}

func pMapErrPreserveOrder(f func(Employer) (Employer, error), list []Employer, worker int) ([]Employer, error) {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapErrNoOrder(f func(Employer) (Employer, error), list []Employer, worker int) ([]Employer, error) {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	return newList, nil
}

func FilterMap(fFilter func(Employer) bool, fMap func(Employer) Employer, list []Employer) []Employer {
	if fFilter == nil || fMap == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

func FilterMapPtr(fFilter func(*Employer) bool, fMap func(*Employer) *Employer, list []*Employer) []*Employer {
	if fFilter == nil || fMap == nil {
		return []*Employer{}
	}
	var newList []*Employer
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapPtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input(*Employer) and returns (bool, error).
//	2. Function: takes *Employer as argument and returns (*Employer, error)
// 	3. Slice of type []*Employer
//
// Returns:
//	New List ([]*Employer, error).
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapPtrErr(fFilter func(*Employer) (bool, error), fMap func(*Employer) (*Employer, error), list []*Employer) ([]*Employer, error) {
	if fFilter == nil || fMap == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input(Employer) and returns (bool, error).
//	2. Function: takes Employer as argument and returns (Employer, error)
// 	3. Slice of type []Employer
//
// Returns:
//	New List ([]Employer, error).
//  Empty list if all there parameters are nil or either of parameter is nil
//
func FilterMapErr(fFilter func(Employer) (bool, error), fMap func(Employer) (Employer, error), list []Employer) ([]Employer, error) {
	if fFilter == nil || fMap == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

func Rest(l []Employer) []Employer {
	if l == nil {
		return []Employer{}
	}

	len := len(l)
	if len == 0 || len == 1 {
		return []Employer{}
	}

	newList := make([]Employer, len-1)

	for i, v := range l[1:] {
		newList[i] = v
	}

	return newList
}


// Rest removes 1st item of the list and return new list having rest of the items
func RestPtr(l []*Employer) []*Employer {
	if l == nil {
		return []*Employer{}
	}

	len := len(l)
	if len == 0 || len == 1 {
		return []*Employer{}
	}

	newList := make([]*Employer, len-1)

	for i, v := range l[1:] {
		newList[i] = v
	}

	return newList
}

func Reduce(f func(Employer, Employer) Employer, list []Employer, initializer ...Employer) Employer {
	var init Employer 
	lenList := len(list)

	if initializer != nil {
		init = initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0]
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}
	
	if lenList == 0 {
		return init
	}
	r := f(init, list[0])
	return Reduce(f, list[1:], r)
}

func ReducePtr(f func(*Employer, *Employer) *Employer, list []*Employer, initializer ...Employer) *Employer {
	var initVal Employer
	var init *Employer = &initVal
	lenList := len(list)

	if initializer != nil {
		init = &initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0]
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init
	}

	r := f(init, list[0])
	return ReducePtr(f, list[1:], *r)
}

// ReducePtrErr reduces a list to a single value by combining elements via a supplied function
//
// Takes three inputs
//	A. function - takes two arguments (*Employer, *Employer)
//	B. list of type []*Employer
// 	C. initializer (optional of type Employer)
//
// Returns:
//	single value, error: (*Employer, error)
func ReducePtrErr(f func(*Employer, *Employer) (*Employer, error), list []*Employer, initializer ...Employer) (*Employer, error) {
	var initVal Employer
	var init *Employer = &initVal
	lenList := len(list)

	if initializer != nil {
		init = &initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0], nil
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init, nil
	}

	r, err := f(init, list[0])
	if err != nil {
		return nil, err
	}
	return ReducePtrErr(f, list[1:], *r)
}

// ReduceErr reduces a list to a single value by combining elements via a supplied function
//
// Takes three inputs
//	A. function - takes two arguments (Employer, Employer)
//	B. list of type []Employer
// 	C. initializer (optional of type Employer)
//
// Returns:
//	single value, error: (Employer, error)
func ReduceErr(f func(Employer, Employer) (Employer, error), list []Employer, initializer ...Employer) (Employer, error) {
	var initVal Employer
	var init Employer = initVal
	lenList := len(list)

	if initializer != nil {
		init = initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0], nil
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init, nil
	}

	r, err := f(init, list[0])
	if err != nil {
		return r, err
	}
	return ReduceErr(f, list[1:], r)
}

// DropLast drops last item from the list and returns new list.
// Returns empty list if there is only one item in the list or list empty
func DropLast(list []Employer) []Employer {
	listLen := len(list)

	if list == nil || listLen == 0 || listLen == 1 {
		return []Employer{}
	}

	newList := make([]Employer, listLen-1)

	for i := 0; i < listLen-1; i++ {
		newList[i] = list[i]
	}
	return newList
}

// DropLastPtr drops last item from the list and returns new list.
// Returns empty list if there is only one item in the list or list empty
func DropLastPtr(list []*Employer) []*Employer {
	listLen := len(list)

	if list == nil || listLen == 0 || listLen == 1 {
		return []*Employer{}
	}

	newList := make([]*Employer, listLen-1)

	for i := 0; i < listLen-1; i++ {
		newList[i] = list[i]
	}
	return newList
}

// Reverses reverse the list
func Reverses(list []Employer) []Employer {
	newList := make([]Employer, len(list))
	for i := 0; i < len(list); i++ {
		newList[i] = list[len(list)-(i+1)]
	}
	return newList
}

// ReversesPtr reverse the list
func ReversesPtr(list []*Employer) []*Employer {
	newList := make([]*Employer, len(list))
	for i := 0; i < len(list); i++ {
		newList[i] = list[len(list)-(i+1)]
	}
	return newList
}

// Take returns n items in the list
func Take(n int, list []Employer) []Employer {
	if n < 0 {
		return []Employer{}
	}

	newListLen := len(list)

	if n < newListLen {
		newListLen = n
	}
	newList := make([]Employer, newListLen)
	for i := 0; i < newListLen; i++ {
		newList[i] = list[i]
	}
	return newList
}

// TakePtr returns n items in the list
func TakePtr(n int, list []*Employer) []*Employer {
	if n < 0 {
		return []*Employer{}
	}

	newListLen := len(list)

	if n < newListLen {
		newListLen = n
	}
	newList := make([]*Employer, newListLen)
	for i := 0; i < newListLen; i++ {
		newList[i] = list[i]
	}
	return newList
}

type employerSlice []Employer
type employerFunctorForMap func(Employer) Employer
type employerFunctorForFilter func(Employer) bool

type employerSlicePtr []*Employer
type employerFunctorForMapPtr func(*Employer) *Employer
type employerFunctorForFilterPtr func(*Employer) bool

// MakeEmployerSlice - creates slice for the functional method such as map, filter
func MakeEmployerSlice(values ...Employer) employerSlice {
	newSlice := employerSlice(values)
	return newSlice
}

// Map - applies the function(1st argument) on each item of the list and returns new list
func (slice employerSlice) Map(functors ...employerFunctorForMap) employerSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = Map(f, tmpSlice)
	}

	return tmpSlice
}

// MakeEmployerSlicePtr - creates slice for the functional method such as map, filter
func MakeEmployerSlicePtr(values ...*Employer) employerSlicePtr {
	newSlice := employerSlicePtr(values)
	return newSlice
}

// MapPtr - applies the function(1st argument) on each item of the list and returns new list
func (slice employerSlicePtr) MapPtr(functors ...employerFunctorForMapPtr) employerSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = MapPtr(f, tmpSlice)
	}

	return tmpSlice
}

// Filter - 
func (slice employerSlice) Filter(functors ...employerFunctorForFilter) employerSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = Filter(f, tmpSlice)
	}

	return tmpSlice
}

// FilterPtr - 
func (slice employerSlicePtr) FilterPtr(functors ...employerFunctorForFilterPtr) employerSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = FilterPtr(f, tmpSlice)
	}

	return tmpSlice
}

// Remove - removes the items from the given list based on supplied function and returns new list
func (slice employerSlice) Remove(functors ...employerFunctorForFilter) employerSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = Remove(f, tmpSlice)
	}

	return tmpSlice
}

// RemovePtr - removes the items from the given list based on supplied function and returns new list
func (slice employerSlicePtr) RemovePtr(functors ...employerFunctorForFilterPtr) employerSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = RemovePtr(f, tmpSlice)
	}

	return tmpSlice
}

// DropWhile - drops the items from the list as long as condition satisfies
func (slice employerSlice) DropWhile(functors ...employerFunctorForFilter) employerSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = DropWhile(f, tmpSlice)
	}

	return tmpSlice
}

// DropWhilePtr - drops the items from the list as long as condition satisfies
func (slice employerSlicePtr) DropWhilePtr(functors ...employerFunctorForFilterPtr) employerSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = DropWhilePtr(f, tmpSlice)
	}

	return tmpSlice
}

// DistinctP returns true if no two of the arguments are =
func DistinctP(list []Employer) bool {
	if len(list) == 0 {
		return false
	}

	s := make(map[Employer]bool)
	for _, v := range list {
		if _, ok := s[v]; ok {
			return false
		}
		s[v] = true
	}
	return true
}

// DistinctPPtr returns true if no two of the arguments are =
func DistinctPPtr(list []*Employer) bool {
	if len(list) == 0 {
		return false
	}

	s := make(map[Employer]bool)
	for _, v := range list {
		if _, ok := s[*v]; ok {
			return false
		}
		s[*v] = true
	}
	return true
}

// Distinct removes duplicates.
func Distinct(list []Employer) []Employer {
	var newList []Employer
	s := make(map[Employer]struct{}, len(list))
	for _, v := range list {
		if _, ok := s[v]; ok {
			continue
		}
		s[v] = struct{}{}
		newList = append(newList, v)
	}
	return newList
}

// DistinctPtr removes duplicates.
func DistinctPtr(list []*Employer) []*Employer {
	var newList []*Employer
	s := make(map[Employer]struct{}, len(list))
	for _, v := range list {
		if _, ok := s[*v]; ok {
			continue
		}
		s[*v] = struct{}{}
		newList = append(newList, v)
	}
	return newList
}

// Union return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func Union(arrList ...[]Employer) []Employer {
	resultMap := make(map[Employer]struct{})
	for _, arr := range arrList {
		for _, v := range arr {
			resultMap[v] = struct{}{}
		}
	}

	resultArr := make([]Employer, len(resultMap))
	i := 0
	for k := range resultMap {
		resultArr[i] = k
		i++
	}
	return resultArr
}

// UnionPtr return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionPtr(arrList ...[]*Employer) []*Employer {
	resultMap := make(map[Employer]struct{})
	var resultArr []*Employer
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[*v]
			if !ok {
				resultMap[*v] = struct{}{}
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// Intersection return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func Intersection(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[Employer]struct{})
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i] == v {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// IntersectionPtr return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionPtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[Employer]struct{})
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				resultMap[*arrList[0][i]] = struct{}{}
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if *arrList[0][i] == *v {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[*arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// Difference returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func Difference(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[Employer]struct{})
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i] == v {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// DifferencePtr returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferencePtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[Employer]struct{})
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				resultMap[*arrList[0][i]] = struct{}{}
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if *arrList[0][i] == *v {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[*arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// Subset returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func Subset(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[Employer]struct{})
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i]]
		if !ok {
			found := false
			resultMap[list1[i]] = struct{}{}
			for j := 0; j < len(list2); j++ {
				if list1[i] == list2[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SubsetPtr returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetPtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[Employer]struct{})
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[*list1[i]]
		if !ok {
			found := false
			resultMap[*list1[i]] = struct{}{}
			for j := 0; j < len(list2); j++ {
				if list1[i] == list2[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// Superset returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func Superset(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[Employer]struct{})

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i]]
		if !ok {
			found := false
			resultMap[list2[i]] = struct{}{}
			for j := 0; j < len(list1); j++ {
				if list2[i] == list1[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetPtr returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetPtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[Employer]struct{})

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[*list2[i]]
		if !ok {
			found := false
			resultMap[*list2[i]] = struct{}{}
			for j := 0; j < len(list1); j++ {
				if list2[i] == list1[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// Set returns a set of the distinct elements of coll.
func Set(list []Employer) []Employer {
	if list == nil || len(list) == 0 {
		return []Employer{}
	}

	resultMap := make(map[Employer]struct{})
	newList := []Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i]]
		if !ok {
			resultMap[list[i]] = struct{}{}
			newList = append(newList, list[i])
		}
	}
	return newList
}

// SetPtr returns a set of the distinct elements of coll.
func SetPtr(list []*Employer) []*Employer {
	if list == nil || len(list) == 0 {
		return []*Employer{}
	}

	resultMap := make(map[Employer]struct{})
	newList := []*Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[*list[i]]
		if !ok {
			resultMap[*list[i]] = struct{}{}
			newList = append(newList, list[i])
		}
	}
	return newList
}

func MapEmployee(f func(employee.Employee) employee.Employee, list []employee.Employee) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

func MapEmployeePtr(f func(*employee.Employee) *employee.Employee, list []*employee.Employee) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployeePtrErr takes 2 arguments:
//  1. A function input argument: *employee.Employee and return types (*employee.Employee, error)
//  2. A list of type []*employee.Employee
//
// Returns:
// 	([]*employee.Employee, error)
func MapEmployeePtrErr(f func(*employee.Employee) (*employee.Employee, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		var err error
		newList[i], err = f(v)
		if err != nil {
			return nil, err
		}
	}
	return newList, nil
}

// MapEmployeeErr takes 2 arguments:
//  1. A function input argument: employee.Employee and return types (employee.Employee, error)
//  2. A list of type []employee.Employee
//
// Returns:
// 	([]employee.Employee, error)
func MapEmployeeErr(f func(employee.Employee) (employee.Employee, error), list []employee.Employee) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		var err error
		newList[i], err = f(v)
		if err != nil {
			return nil, err
		}
	}
	return newList, nil
}

func FilterEmployee(f func(employee.Employee) bool, list []employee.Employee) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

func FilterEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	var newList []*employee.Employee
	for _, v := range list {
		if f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

// FilterEmployeePtrErr takes two arguments
//  1. Function: takes 1 argument of type employee.Employee and returns (bool, error)
//  2. slice of type []*employee.Employee
//
// Returns:
//  new filtered list and error
func FilterEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

// FilterEmployeeErr takes two arguments
//  1. Function: takes 1 argument of type employee.Employee and returns (bool, error)
//  2. slice of type []employee.Employee
//
// Returns:
//  new filtered list and error
func FilterEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

func RemoveEmployee(f func(employee.Employee) bool, list []employee.Employee) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if !f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

func RemoveEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	var newList []*employee.Employee
	for _, v := range list {
		if !f(v) {
			newList = append(newList, v)
		}
	}
	return newList
}

// RemoveEmployeePtrErr removes the items from the given list based on supplied function and returns new list
//
// Takes 2 inputs:
//	1. Function: input type *employee.Employee and return types(bool, error)
//	2. List of type: []*employee.Employee
//
// Returns:
//	New list and error: ([]*employee.Employee, error)
//	Empty list if both of arguments are nil or either one is nil.
func RemoveEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

// RemoveEmployeeErr removes the items from the given list based on supplied function and returns new list
//
// Takes 2 inputs:
//	1. Function: input type employee.Employee and return types(bool, error)
//	2. List of type: []employee.Employee
//
// Returns:
//	New list and error: ([]employee.Employee, error)
//	Empty list if both of arguments are nil or either one is nil.
func RemoveEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			newList = append(newList, v)
		}
	}
	return newList, nil
}

func SomeEmployee(f func(employee.Employee) bool, list []employee.Employee) bool {
	if f == nil {
		return false
	}
	for _, v := range list {
		if f(v) {
			return true
		}
	}
	return false
}

func SomeEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) bool {
	if f == nil {
		return false
	}
	for _, v := range list {
		if f(v) {
			return true
		}
	}
	return false
}

// SomeEmployeePtrErr finds item in the list based on supplied function.
//
// Takes 2 input:
//	1. Function
//	2. List
//
// Returns:
//	(bool,err).
//	True if condition satisfies, else false
func SomeEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) (bool, error) {
	if f == nil {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if r {
			return true, nil
		}
	}
	return false, nil
}

// SomeEmployeeErr finds item in the list based on supplied function.
//
// Takes 2 input:
//	1. Function
//	2. List
//
// Returns:
//	(bool,err).
//	True if condition satisfies, else false
func SomeEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) (bool, error) {
	if f == nil {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if r {
			return true, nil
		}
	}
	return false, nil
}

func EveryEmployee(f func(employee.Employee) bool, list []employee.Employee) bool {
	if f == nil || len(list) == 0 {
		return false
	}
	for _, v := range list {
		if !f(v) {
			return false
		}
	}
	return true
}

func EveryEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) bool {
	if f == nil || len(list) == 0 {
		return false
	}
	for _, v := range list {
		if !f(v) {
			return false
		}
	}
	return true
}

// EveryEmployeePtrErr returns true if supplied function returns logical true for every item in the list
func EveryEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) (bool, error) {
	if f == nil || len(list) == 0 {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if !r {
			return false, nil
		}
	}
	return true, nil
}

// EveryEmployeeErr returns true if supplied function returns logical true for every item in the list
func EveryEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) (bool, error) {
	if f == nil || len(list) == 0 {
		return false, nil
	}
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return false, err
		}
		if !r {
			return false, nil
		}
	}
	return true, nil
}

func DropWhileEmployee(f func(employee.Employee) bool, list []employee.Employee) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for i, v := range list {
		if !f(v) {
			listLen := len(list)
			newList = make([]employee.Employee, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList
		}
	}
	return newList
}

func DropWhileEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	var newList []*employee.Employee
	for i, v := range list {
		if !f(v) {
			listLen := len(list)
			newList = make([]*employee.Employee, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList
		}
	}
	return newList
}

// DropWhileEmployeePtrErr drops the items from the list as long as condition satisfies.
//
// Takes two inputs
//	1. Function: takes one input and returns (boolean, error)
//	2. list
//
// Returns:
// 	New List, error
//  Empty list if either one of arguments or both of them are nil
func DropWhileEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			listLen := len(list)
			newList = make([]*employee.Employee, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList, nil
		}
	}
	return newList, nil
}

// DropWhileEmployeeErr drops the items from the list as long as condition satisfies.
//
// Takes two inputs
//	1. Function: takes one input and returns (boolean, error)
//	2. list
//
// Returns:
// 	New List, error
//  Empty list if either one of arguments or both of them are nil
func DropWhileEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			listLen := len(list)
			newList = make([]employee.Employee, listLen-i)
			j := 0
			for i < listLen {
				newList[j] = list[i]
				i++
				j++
			}
			return newList, nil
		}
	}
	return newList, nil
}

func TakeWhileEmployee(f func(employee.Employee) bool, list []employee.Employee) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if !f(v) {
			return newList
		}
		newList = append(newList, v)
	}
	return newList
}

func TakeWhileEmployeePtr(f func(*employee.Employee) bool, list []*employee.Employee) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	var newList []*employee.Employee
	for _, v := range list {
		if !f(v) {
			return newList
		}
		newList = append(newList, v)
	}
	return newList
}

// TakeWhileEmployeePtrErr returns new list based on condition in the supplied function. It returns new list once condition fails.
//
// Takes 2 inputs:
//	1. Function - 1 input of type *employee.Employee and returns ([]*employee.Employee, error)
//	2. List
//
// Returns:
//	New List, error.
//	Empty list if all the parameters are nil or either of one parameter is nil
func TakeWhileEmployeePtrErr(f func(*employee.Employee) (bool, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			return newList, nil
		}
		newList = append(newList, v)
	}
	return newList, nil
}

// TakeWhileEmployeeErr returns new list based on condition in the supplied function. It returns new list once condition fails.
//
// Takes 2 inputs:
//	1. Function - 1 input of type employee.Employee and returns ([]employee.Employee, error)
//	2. List
//
// Returns:
//	New List, error.
//	Empty list if all the parameters are nil or either of one parameter is nil
func TakeWhileEmployeeErr(f func(employee.Employee) (bool, error), list []employee.Employee) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		if !r {
			return newList, nil
		}
		newList = append(newList, v)
	}
	return newList, nil
}

// PMapEmployee applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployee(f func(employee.Employee) employee.Employee, list []employee.Employee, optional ...fp.Optional) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeNoOrder(f, list, worker)
		}
	}

	return pMapEmployeePreserveOrder(f, list, worker)
}

func pMapEmployeePreserveOrder(f func(employee.Employee) employee.Employee, list []employee.Employee, worker int) []employee.Employee {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeeNoOrder(f func(employee.Employee) employee.Employee, list []employee.Employee, worker int) []employee.Employee {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeePtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeePtr(f func(*employee.Employee) *employee.Employee, list []*employee.Employee, optional ...fp.Optional) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeePtrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeePtrPreserveOrder(f, list, worker)
}

func pMapEmployeePtrPreserveOrder(f func(*employee.Employee) *employee.Employee, list []*employee.Employee, worker int) []*employee.Employee {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeePtrNoOrder(f func(*employee.Employee) *employee.Employee, list []*employee.Employee, worker int) []*employee.Employee {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeePtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeePtrErr(f func(*employee.Employee) (*employee.Employee, error), list []*employee.Employee, optional ...fp.Optional) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeePtrErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeePtrErrPreserveOrder(f, list, worker)
}

func pMapEmployeePtrErrPreserveOrder(f func(*employee.Employee) (*employee.Employee, error), list []*employee.Employee, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeePtrErrNoOrder(f func(*employee.Employee) (*employee.Employee, error), list []*employee.Employee, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	return newList, nil
}

// PMapEmployeeErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeErr(f func(employee.Employee) (employee.Employee, error), list []employee.Employee, optional ...fp.Optional) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeErrPreserveOrder(f, list, worker)
}

func pMapEmployeeErrPreserveOrder(f func(employee.Employee) (employee.Employee, error), list []employee.Employee, worker int) ([]employee.Employee, error) {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeeErrNoOrder(f func(employee.Employee) (employee.Employee, error), list []employee.Employee, worker int) ([]employee.Employee, error) {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	return newList, nil
}

func FilterMapEmployee(fFilter func(employee.Employee) bool, fMap func(employee.Employee) employee.Employee, list []employee.Employee) []employee.Employee {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

func FilterMapEmployeePtr(fFilter func(*employee.Employee) bool, fMap func(*employee.Employee) *employee.Employee, list []*employee.Employee) []*employee.Employee {
	if fFilter == nil || fMap == nil {
		return []*employee.Employee{}
	}
	var newList []*employee.Employee
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapEmployeePtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input(*employee.Employee) and returns (bool, error).
//	2. Function: takes *employee.Employee as argument and returns (*employee.Employee, error)
// 	3. Slice of type []*employee.Employee
//
// Returns:
//	New List ([]*employee.Employee, error).
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeePtrErr(fFilter func(*employee.Employee) (bool, error), fMap func(*employee.Employee) (*employee.Employee, error), list []*employee.Employee) ([]*employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapEmployeeErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input(employee.Employee) and returns (bool, error).
//	2. Function: takes employee.Employee as argument and returns (employee.Employee, error)
// 	3. Slice of type []employee.Employee
//
// Returns:
//	New List ([]employee.Employee, error).
//  Empty list if all there parameters are nil or either of parameter is nil
//
func FilterMapEmployeeErr(fFilter func(employee.Employee) (bool, error), fMap func(employee.Employee) (employee.Employee, error), list []employee.Employee) ([]employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

func RestEmployee(l []employee.Employee) []employee.Employee {
	if l == nil {
		return []employee.Employee{}
	}

	len := len(l)
	if len == 0 || len == 1 {
		return []employee.Employee{}
	}

	newList := make([]employee.Employee, len-1)

	for i, v := range l[1:] {
		newList[i] = v
	}

	return newList
}


// RestEmployee removes 1st item of the list and return new list having rest of the items
func RestEmployeePtr(l []*employee.Employee) []*employee.Employee {
	if l == nil {
		return []*employee.Employee{}
	}

	len := len(l)
	if len == 0 || len == 1 {
		return []*employee.Employee{}
	}

	newList := make([]*employee.Employee, len-1)

	for i, v := range l[1:] {
		newList[i] = v
	}

	return newList
}

func ReduceEmployee(f func(employee.Employee, employee.Employee) employee.Employee, list []employee.Employee, initializer ...employee.Employee) employee.Employee {
	var init employee.Employee 
	lenList := len(list)

	if initializer != nil {
		init = initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0]
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}
	
	if lenList == 0 {
		return init
	}
	r := f(init, list[0])
	return ReduceEmployee(f, list[1:], r)
}

func ReduceEmployeePtr(f func(*employee.Employee, *employee.Employee) *employee.Employee, list []*employee.Employee, initializer ...employee.Employee) *employee.Employee {
	var initVal employee.Employee
	var init *employee.Employee = &initVal
	lenList := len(list)

	if initializer != nil {
		init = &initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0]
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init
	}

	r := f(init, list[0])
	return ReduceEmployeePtr(f, list[1:], *r)
}

// ReduceEmployeePtrErr reduces a list to a single value by combining elements via a supplied function
//
// Takes three inputs
//	A. function - takes two arguments (*employee.Employee, *employee.Employee)
//	B. list of type []*employee.Employee
// 	C. initializer (optional of type employee.Employee)
//
// Returns:
//	single value, error: (*employee.Employee, error)
func ReduceEmployeePtrErr(f func(*employee.Employee, *employee.Employee) (*employee.Employee, error), list []*employee.Employee, initializer ...employee.Employee) (*employee.Employee, error) {
	var initVal employee.Employee
	var init *employee.Employee = &initVal
	lenList := len(list)

	if initializer != nil {
		init = &initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0], nil
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init, nil
	}

	r, err := f(init, list[0])
	if err != nil {
		return nil, err
	}
	return ReduceEmployeePtrErr(f, list[1:], *r)
}

// ReduceEmployeeErr reduces a list to a single value by combining elements via a supplied function
//
// Takes three inputs
//	A. function - takes two arguments (employee.Employee, employee.Employee)
//	B. list of type []employee.Employee
// 	C. initializer (optional of type employee.Employee)
//
// Returns:
//	single value, error: (employee.Employee, error)
func ReduceEmployeeErr(f func(employee.Employee, employee.Employee) (employee.Employee, error), list []employee.Employee, initializer ...employee.Employee) (employee.Employee, error) {
	var initVal employee.Employee
	var init employee.Employee = initVal
	lenList := len(list)

	if initializer != nil {
		init = initializer[0]
	} else if lenList > 0 {
		init = list[0]
		if lenList == 1 {
			return list[0], nil
		}
		if lenList >= 2 {
			list = list[1:]
		}
	}

	if lenList == 0 {
		return init, nil
	}

	r, err := f(init, list[0])
	if err != nil {
		return r, err
	}
	return ReduceEmployeeErr(f, list[1:], r)
}

// DropLastEmployee drops last item from the list and returns new list.
// Returns empty list if there is only one item in the list or list empty
func DropLastEmployee(list []employee.Employee) []employee.Employee {
	listLen := len(list)

	if list == nil || listLen == 0 || listLen == 1 {
		return []employee.Employee{}
	}

	newList := make([]employee.Employee, listLen-1)

	for i := 0; i < listLen-1; i++ {
		newList[i] = list[i]
	}
	return newList
}

// DropLastEmployeePtr drops last item from the list and returns new list.
// Returns empty list if there is only one item in the list or list empty
func DropLastEmployeePtr(list []*employee.Employee) []*employee.Employee {
	listLen := len(list)

	if list == nil || listLen == 0 || listLen == 1 {
		return []*employee.Employee{}
	}

	newList := make([]*employee.Employee, listLen-1)

	for i := 0; i < listLen-1; i++ {
		newList[i] = list[i]
	}
	return newList
}

// ReverseEmployees reverse the list
func ReverseEmployees(list []employee.Employee) []employee.Employee {
	newList := make([]employee.Employee, len(list))
	for i := 0; i < len(list); i++ {
		newList[i] = list[len(list)-(i+1)]
	}
	return newList
}

// ReverseEmployeesPtr reverse the list
func ReverseEmployeesPtr(list []*employee.Employee) []*employee.Employee {
	newList := make([]*employee.Employee, len(list))
	for i := 0; i < len(list); i++ {
		newList[i] = list[len(list)-(i+1)]
	}
	return newList
}

// TakeEmployee returns n items in the list
func TakeEmployee(n int, list []employee.Employee) []employee.Employee {
	if n < 0 {
		return []employee.Employee{}
	}

	newListLen := len(list)

	if n < newListLen {
		newListLen = n
	}
	newList := make([]employee.Employee, newListLen)
	for i := 0; i < newListLen; i++ {
		newList[i] = list[i]
	}
	return newList
}

// TakeEmployeePtr returns n items in the list
func TakeEmployeePtr(n int, list []*employee.Employee) []*employee.Employee {
	if n < 0 {
		return []*employee.Employee{}
	}

	newListLen := len(list)

	if n < newListLen {
		newListLen = n
	}
	newList := make([]*employee.Employee, newListLen)
	for i := 0; i < newListLen; i++ {
		newList[i] = list[i]
	}
	return newList
}

type employeeSlice []employee.Employee
type employeeFunctorForMap func(employee.Employee) employee.Employee
type employeeFunctorForFilter func(employee.Employee) bool

type employeeSlicePtr []*employee.Employee
type employeeFunctorForMapPtr func(*employee.Employee) *employee.Employee
type employeeFunctorForFilterPtr func(*employee.Employee) bool

// MakeEmployeeSlice - creates slice for the functional method such as map, filter
func MakeEmployeeSlice(values ...employee.Employee) employeeSlice {
	newSlice := employeeSlice(values)
	return newSlice
}

// Map - applies the function(1st argument) on each item of the list and returns new list
func (slice employeeSlice) Map(functors ...employeeFunctorForMap) employeeSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = MapEmployee(f, tmpSlice)
	}

	return tmpSlice
}

// MakeEmployeeSlicePtr - creates slice for the functional method such as map, filter
func MakeEmployeeSlicePtr(values ...*employee.Employee) employeeSlicePtr {
	newSlice := employeeSlicePtr(values)
	return newSlice
}

// MapPtr - applies the function(1st argument) on each item of the list and returns new list
func (slice employeeSlicePtr) MapPtr(functors ...employeeFunctorForMapPtr) employeeSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = MapEmployeePtr(f, tmpSlice)
	}

	return tmpSlice
}

// Filter - 
func (slice employeeSlice) Filter(functors ...employeeFunctorForFilter) employeeSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = FilterEmployee(f, tmpSlice)
	}

	return tmpSlice
}

// FilterPtr - 
func (slice employeeSlicePtr) FilterPtr(functors ...employeeFunctorForFilterPtr) employeeSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = FilterEmployeePtr(f, tmpSlice)
	}

	return tmpSlice
}

// Remove - removes the items from the given list based on supplied function and returns new list
func (slice employeeSlice) Remove(functors ...employeeFunctorForFilter) employeeSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = RemoveEmployee(f, tmpSlice)
	}

	return tmpSlice
}

// RemovePtr - removes the items from the given list based on supplied function and returns new list
func (slice employeeSlicePtr) RemovePtr(functors ...employeeFunctorForFilterPtr) employeeSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = RemoveEmployeePtr(f, tmpSlice)
	}

	return tmpSlice
}

// DropWhile - drops the items from the list as long as condition satisfies
func (slice employeeSlice) DropWhile(functors ...employeeFunctorForFilter) employeeSlice {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = DropWhileEmployee(f, tmpSlice)
	}

	return tmpSlice
}

// DropWhilePtr - drops the items from the list as long as condition satisfies
func (slice employeeSlicePtr) DropWhilePtr(functors ...employeeFunctorForFilterPtr) employeeSlicePtr {

	tmpSlice := slice
	
	for _, f := range functors {
		if f == nil {
			continue
		}
		tmpSlice = DropWhileEmployeePtr(f, tmpSlice)
	}

	return tmpSlice
}

// DistinctEmployeeP returns true if no two of the arguments are =
func DistinctEmployeeP(list []employee.Employee) bool {
	if len(list) == 0 {
		return false
	}

	s := make(map[employee.Employee]bool)
	for _, v := range list {
		if _, ok := s[v]; ok {
			return false
		}
		s[v] = true
	}
	return true
}

// DistinctEmployeePPtr returns true if no two of the arguments are =
func DistinctEmployeePPtr(list []*employee.Employee) bool {
	if len(list) == 0 {
		return false
	}

	s := make(map[employee.Employee]bool)
	for _, v := range list {
		if _, ok := s[*v]; ok {
			return false
		}
		s[*v] = true
	}
	return true
}

// DistinctEmployee removes duplicates.
func DistinctEmployee(list []employee.Employee) []employee.Employee {
	var newList []employee.Employee
	s := make(map[employee.Employee]struct{}, len(list))
	for _, v := range list {
		if _, ok := s[v]; ok {
			continue
		}
		s[v] = struct{}{}
		newList = append(newList, v)
	}
	return newList
}

// DistinctEmployeePtr removes duplicates.
func DistinctEmployeePtr(list []*employee.Employee) []*employee.Employee {
	var newList []*employee.Employee
	s := make(map[employee.Employee]struct{}, len(list))
	for _, v := range list {
		if _, ok := s[*v]; ok {
			continue
		}
		s[*v] = struct{}{}
		newList = append(newList, v)
	}
	return newList
}

// UnionEmployee return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployee(arrList ...[]employee.Employee) []employee.Employee {
	resultMap := make(map[employee.Employee]struct{})
	for _, arr := range arrList {
		for _, v := range arr {
			resultMap[v] = struct{}{}
		}
	}

	resultArr := make([]employee.Employee, len(resultMap))
	i := 0
	for k := range resultMap {
		resultArr[i] = k
		i++
	}
	return resultArr
}

// UnionEmployeePtr return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployeePtr(arrList ...[]*employee.Employee) []*employee.Employee {
	resultMap := make(map[employee.Employee]struct{})
	var resultArr []*employee.Employee
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[*v]
			if !ok {
				resultMap[*v] = struct{}{}
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// IntersectionEmployee return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployee(arrList ...[]employee.Employee) []employee.Employee {
	if arrList == nil {
		return []employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	if len(arrList) == 1 {
		var newList []employee.Employee
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
		return newList
	}

	var newList []employee.Employee
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i] == v {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// IntersectionEmployeePtr return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployeePtr(arrList ...[]*employee.Employee) []*employee.Employee {
	if arrList == nil {
		return []*employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	if len(arrList) == 1 {
		var newList []*employee.Employee
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				resultMap[*arrList[0][i]] = struct{}{}
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*employee.Employee
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if *arrList[0][i] == *v {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[*arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// DifferenceEmployee returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployee(arrList ...[]employee.Employee) []employee.Employee {
	if arrList == nil {
		return []employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	if len(arrList) == 1 {
		var newList []employee.Employee
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
		return newList
	}

	var newList []employee.Employee
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i] == v {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// DifferenceEmployeePtr returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployeePtr(arrList ...[]*employee.Employee) []*employee.Employee {
	if arrList == nil {
		return []*employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	if len(arrList) == 1 {
		var newList []*employee.Employee
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				resultMap[*arrList[0][i]] = struct{}{}
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*employee.Employee
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if *arrList[0][i] == *v {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[*arrList[0][i]]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[*arrList[0][i]] = struct{}{}
			}
		}
	}
	return newList
}

// SubsetEmployee returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployee(list1, list2 []employee.Employee) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[employee.Employee]struct{})
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i]]
		if !ok {
			found := false
			resultMap[list1[i]] = struct{}{}
			for j := 0; j < len(list2); j++ {
				if list1[i] == list2[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SubsetEmployeePtr returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployeePtr(list1, list2 []*employee.Employee) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[employee.Employee]struct{})
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[*list1[i]]
		if !ok {
			found := false
			resultMap[*list1[i]] = struct{}{}
			for j := 0; j < len(list2); j++ {
				if list1[i] == list2[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployee returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployee(list1, list2 []employee.Employee) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[employee.Employee]struct{})

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i]]
		if !ok {
			found := false
			resultMap[list2[i]] = struct{}{}
			for j := 0; j < len(list1); j++ {
				if list2[i] == list1[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployeePtr returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployeePtr(list1, list2 []*employee.Employee) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[employee.Employee]struct{})

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[*list2[i]]
		if !ok {
			found := false
			resultMap[*list2[i]] = struct{}{}
			for j := 0; j < len(list1); j++ {
				if list2[i] == list1[j] {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SetEmployee returns a set of the distinct elements of coll.
func SetEmployee(list []employee.Employee) []employee.Employee {
	if list == nil || len(list) == 0 {
		return []employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	newList := []employee.Employee{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i]]
		if !ok {
			resultMap[list[i]] = struct{}{}
			newList = append(newList, list[i])
		}
	}
	return newList
}

// SetEmployeePtr returns a set of the distinct elements of coll.
func SetEmployeePtr(list []*employee.Employee) []*employee.Employee {
	if list == nil || len(list) == 0 {
		return []*employee.Employee{}
	}

	resultMap := make(map[employee.Employee]struct{})
	newList := []*employee.Employee{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[*list[i]]
		if !ok {
			resultMap[*list[i]] = struct{}{}
			newList = append(newList, list[i])
		}
	}
	return newList
}


// MapEmployerEmployee takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployerEmployee(f func(Employer) employee.Employee, list []Employer) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployerEmployeeErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapEmployerEmployeeErr(f func(Employer) (employee.Employee, error), list []Employer) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapEmployerEmployeePtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployerEmployeePtr(f func(*Employer) *employee.Employee, list []*Employer) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployerEmployeePtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapEmployerEmployeePtrErr(f func(*Employer) (*employee.Employee, error), list []*Employer) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapEmployerEmployee applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerEmployee(f func(Employer) employee.Employee, list []Employer, optional ...fp.Optional) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerEmployeeNoOrder(f, list, worker)
		}
	}

	return pMapEmployerEmployeePreserveOrder(f, list, worker)
}

func pMapEmployerEmployeePreserveOrder(f func(Employer) employee.Employee, list []Employer, worker int) []employee.Employee {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployerEmployeeNoOrder(f func(Employer) employee.Employee, list []Employer, worker int) []employee.Employee {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployerEmployeeErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerEmployeeErr(f func(Employer) (employee.Employee, error), list []Employer, optional ...fp.Optional) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerEmployeeErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerEmployeeErrPreserveOrder(f, list, worker)
}

func pMapEmployerEmployeeErrPreserveOrder(f func(Employer) (employee.Employee, error), list []Employer, worker int) ([]employee.Employee, error) {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployerEmployeeErrNoOrder(f func(Employer) (employee.Employee, error), list []Employer, worker int) ([]employee.Employee, error) {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	return newList, nil
}

// PMapEmployerEmployeePtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerEmployeePtr(f func(*Employer) *employee.Employee, list []*Employer, optional ...fp.Optional) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerEmployeePtrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerEmployeePtrPreserveOrder(f, list, worker)
}

func pMapEmployerEmployeePtrPreserveOrder(f func(*Employer) *employee.Employee, list []*Employer, worker int) []*employee.Employee {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployerEmployeePtrNoOrder(f func(*Employer) *employee.Employee, list []*Employer, worker int) []*employee.Employee {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployerEmployeePtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerEmployeePtrErr(f func(*Employer) (*employee.Employee, error), list []*Employer, optional ...fp.Optional) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerEmployeePtrErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerEmployeePtrErrPreserveOrder(f, list, worker)
}

func pMapEmployerEmployeePtrErrPreserveOrder(f func(*Employer) (*employee.Employee, error), list []*Employer, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployerEmployeePtrErrNoOrder(f func(*Employer) (*employee.Employee, error), list []*Employer, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	return newList, nil
}

// FilterMapEmployerEmployee filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - Employer and returns true/false.
//	2. Function: takes Employer as argument and returns employee.Employee
// 	3. List of type Employer
//
// Returns:
//	New List of type employee.Employee
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerEmployee(fFilter func(Employer) bool, fMap func(Employer) employee.Employee, list []Employer) []employee.Employee {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapEmployerEmployeePtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *Employer and returns (bool, error).
//	2. Function: takes *Employer as argument and returns (*employee.Employee, error)
// 	3. List of type *Employer
//
// Returns:
//	New List of type *employee.Employee, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerEmployeePtrErr(fFilter func(*Employer) (bool, error), fMap func(*Employer) (*employee.Employee, error), list []*Employer) ([]*employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapEmployerEmployeeErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - Employer and returns (bool, error).
//	2. Function: takes Employer as argument and returns (employee.Employee, error)
// 	3. List of type Employer
//
// Returns:
//	New List of type employee.Employee, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerEmployeeErr(fFilter func(Employer) (bool, error), fMap func(Employer) (employee.Employee, error), list []Employer) ([]employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// MapEmployerInt takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployerInt(f func(Employer) int, list []Employer) []int {
	if f == nil {
		return []int{}
	}
	newList := make([]int, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployerIntErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapEmployerIntErr(f func(Employer) (int, error), list []Employer) ([]int, error) {
	if f == nil {
		return []int{}, nil
	}
	newList := make([]int, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapEmployerIntPtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployerIntPtr(f func(*Employer) *int, list []*Employer) []*int {
	if f == nil {
		return []*int{}
	}
	newList := make([]*int, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployerIntPtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapEmployerIntPtrErr(f func(*Employer) (*int, error), list []*Employer) ([]*int, error) {
	if f == nil {
		return []*int{}, nil
	}
	newList := make([]*int, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapEmployerInt applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerInt(f func(Employer) int, list []Employer, optional ...fp.Optional) []int {
	if f == nil {
		return []int{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerIntNoOrder(f, list, worker)
		}
	}

	return pMapEmployerIntPreserveOrder(f, list, worker)
}

func pMapEmployerIntPreserveOrder(f func(Employer) int, list []Employer, worker int) []int {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]int, chJobs chan map[int]Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]int{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]int, len(list))
	newList := make([]int, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployerIntNoOrder(f func(Employer) int, list []Employer, worker int) []int {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan int, chJobs chan Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]int, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployerIntErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerIntErr(f func(Employer) (int, error), list []Employer, optional ...fp.Optional) ([]int, error) {
	if f == nil {
		return []int{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerIntErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerIntErrPreserveOrder(f, list, worker)
}

func pMapEmployerIntErrPreserveOrder(f func(Employer) (int, error), list []Employer, worker int) ([]int, error) {
	chJobs := make(chan map[int]Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]int, chJobs chan map[int]Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]int{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]int, len(list))
	newList := make([]int, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []int{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []int{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployerIntErrNoOrder(f func(Employer) (int, error), list []Employer, worker int) ([]int, error) {
	chJobs := make(chan Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan int, chJobs chan Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]int, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []int{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []int{}, <-errCh
	}

	return newList, nil
}

// PMapEmployerIntPtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerIntPtr(f func(*Employer) *int, list []*Employer, optional ...fp.Optional) []*int {
	if f == nil {
		return []*int{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerIntPtrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerIntPtrPreserveOrder(f, list, worker)
}

func pMapEmployerIntPtrPreserveOrder(f func(*Employer) *int, list []*Employer, worker int) []*int {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*int, chJobs chan map[int]*Employer) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*int{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*int, len(list))
	newList := make([]*int, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployerIntPtrNoOrder(f func(*Employer) *int, list []*Employer, worker int) []*int {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *int, chJobs chan *Employer) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*int, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployerIntPtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployerIntPtrErr(f func(*Employer) (*int, error), list []*Employer, optional ...fp.Optional) ([]*int, error) {
	if f == nil {
		return []*int{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployerIntPtrErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployerIntPtrErrPreserveOrder(f, list, worker)
}

func pMapEmployerIntPtrErrPreserveOrder(f func(*Employer) (*int, error), list []*Employer, worker int) ([]*int, error) {
	chJobs := make(chan map[int]*Employer, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*Employer{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*int, chJobs chan map[int]*Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*int{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*int, len(list))
	newList := make([]*int, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*int{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*int{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployerIntPtrErrNoOrder(f func(*Employer) (*int, error), list []*Employer, worker int) ([]*int, error) {
	chJobs := make(chan *Employer, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *int, chJobs chan *Employer, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*int, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*int{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*int{}, <-errCh
	}

	return newList, nil
}

// FilterMapEmployerInt filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - Employer and returns true/false.
//	2. Function: takes Employer as argument and returns int
// 	3. List of type Employer
//
// Returns:
//	New List of type int
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerInt(fFilter func(Employer) bool, fMap func(Employer) int, list []Employer) []int {
	if fFilter == nil || fMap == nil {
		return []int{}
	}
	var newList []int
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapEmployerIntPtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *Employer and returns (bool, error).
//	2. Function: takes *Employer as argument and returns (*int, error)
// 	3. List of type *Employer
//
// Returns:
//	New List of type *int, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerIntPtrErr(fFilter func(*Employer) (bool, error), fMap func(*Employer) (*int, error), list []*Employer) ([]*int, error) {
	if fFilter == nil || fMap == nil {
		return []*int{}, nil
	}
	var newList []*int
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapEmployerIntErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - Employer and returns (bool, error).
//	2. Function: takes Employer as argument and returns (int, error)
// 	3. List of type Employer
//
// Returns:
//	New List of type int, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployerIntErr(fFilter func(Employer) (bool, error), fMap func(Employer) (int, error), list []Employer) ([]int, error) {
	if fFilter == nil || fMap == nil {
		return []int{}, nil
	}
	var newList []int
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// MapEmployeeEmployer takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployeeEmployer(f func(employee.Employee) Employer, list []employee.Employee) []Employer {
	if f == nil {
		return []Employer{}
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployeeEmployerErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapEmployeeEmployerErr(f func(employee.Employee) (Employer, error), list []employee.Employee) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapEmployeeEmployerPtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployeeEmployerPtr(f func(*employee.Employee) *Employer, list []*employee.Employee) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployeeEmployerPtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapEmployeeEmployerPtrErr(f func(*employee.Employee) (*Employer, error), list []*employee.Employee) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapEmployeeEmployer applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeEmployer(f func(employee.Employee) Employer, list []employee.Employee, optional ...fp.Optional) []Employer {
	if f == nil {
		return []Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeEmployerNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeEmployerPreserveOrder(f, list, worker)
}

func pMapEmployeeEmployerPreserveOrder(f func(employee.Employee) Employer, list []employee.Employee, worker int) []Employer {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeeEmployerNoOrder(f func(employee.Employee) Employer, list []employee.Employee, worker int) []Employer {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeeEmployerErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeEmployerErr(f func(employee.Employee) (Employer, error), list []employee.Employee, optional ...fp.Optional) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeEmployerErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeEmployerErrPreserveOrder(f, list, worker)
}

func pMapEmployeeEmployerErrPreserveOrder(f func(employee.Employee) (Employer, error), list []employee.Employee, worker int) ([]Employer, error) {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeeEmployerErrNoOrder(f func(employee.Employee) (Employer, error), list []employee.Employee, worker int) ([]Employer, error) {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	return newList, nil
}

// PMapEmployeeEmployerPtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeEmployerPtr(f func(*employee.Employee) *Employer, list []*employee.Employee, optional ...fp.Optional) []*Employer {
	if f == nil {
		return []*Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeEmployerPtrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeEmployerPtrPreserveOrder(f, list, worker)
}

func pMapEmployeeEmployerPtrPreserveOrder(f func(*employee.Employee) *Employer, list []*employee.Employee, worker int) []*Employer {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeeEmployerPtrNoOrder(f func(*employee.Employee) *Employer, list []*employee.Employee, worker int) []*Employer {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeeEmployerPtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeEmployerPtrErr(f func(*employee.Employee) (*Employer, error), list []*employee.Employee, optional ...fp.Optional) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeEmployerPtrErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeEmployerPtrErrPreserveOrder(f, list, worker)
}

func pMapEmployeeEmployerPtrErrPreserveOrder(f func(*employee.Employee) (*Employer, error), list []*employee.Employee, worker int) ([]*Employer, error) {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeeEmployerPtrErrNoOrder(f func(*employee.Employee) (*Employer, error), list []*employee.Employee, worker int) ([]*Employer, error) {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	return newList, nil
}

// FilterMapEmployeeEmployer filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - employee.Employee and returns true/false.
//	2. Function: takes employee.Employee as argument and returns Employer
// 	3. List of type employee.Employee
//
// Returns:
//	New List of type Employer
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeEmployer(fFilter func(employee.Employee) bool, fMap func(employee.Employee) Employer, list []employee.Employee) []Employer {
	if fFilter == nil || fMap == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapEmployeeEmployerPtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *employee.Employee and returns (bool, error).
//	2. Function: takes *employee.Employee as argument and returns (*Employer, error)
// 	3. List of type *employee.Employee
//
// Returns:
//	New List of type *Employer, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeEmployerPtrErr(fFilter func(*employee.Employee) (bool, error), fMap func(*employee.Employee) (*Employer, error), list []*employee.Employee) ([]*Employer, error) {
	if fFilter == nil || fMap == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapEmployeeEmployerErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - employee.Employee and returns (bool, error).
//	2. Function: takes employee.Employee as argument and returns (Employer, error)
// 	3. List of type employee.Employee
//
// Returns:
//	New List of type Employer, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeEmployerErr(fFilter func(employee.Employee) (bool, error), fMap func(employee.Employee) (Employer, error), list []employee.Employee) ([]Employer, error) {
	if fFilter == nil || fMap == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// MapEmployeeInt takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployeeInt(f func(employee.Employee) int, list []employee.Employee) []int {
	if f == nil {
		return []int{}
	}
	newList := make([]int, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployeeIntErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapEmployeeIntErr(f func(employee.Employee) (int, error), list []employee.Employee) ([]int, error) {
	if f == nil {
		return []int{}, nil
	}
	newList := make([]int, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapEmployeeIntPtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapEmployeeIntPtr(f func(*employee.Employee) *int, list []*employee.Employee) []*int {
	if f == nil {
		return []*int{}
	}
	newList := make([]*int, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapEmployeeIntPtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapEmployeeIntPtrErr(f func(*employee.Employee) (*int, error), list []*employee.Employee) ([]*int, error) {
	if f == nil {
		return []*int{}, nil
	}
	newList := make([]*int, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapEmployeeInt applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeInt(f func(employee.Employee) int, list []employee.Employee, optional ...fp.Optional) []int {
	if f == nil {
		return []int{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeIntNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeIntPreserveOrder(f, list, worker)
}

func pMapEmployeeIntPreserveOrder(f func(employee.Employee) int, list []employee.Employee, worker int) []int {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]int, chJobs chan map[int]employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]int{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]int, len(list))
	newList := make([]int, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeeIntNoOrder(f func(employee.Employee) int, list []employee.Employee, worker int) []int {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan int, chJobs chan employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]int, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeeIntErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeIntErr(f func(employee.Employee) (int, error), list []employee.Employee, optional ...fp.Optional) ([]int, error) {
	if f == nil {
		return []int{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeIntErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeIntErrPreserveOrder(f, list, worker)
}

func pMapEmployeeIntErrPreserveOrder(f func(employee.Employee) (int, error), list []employee.Employee, worker int) ([]int, error) {
	chJobs := make(chan map[int]employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]int, chJobs chan map[int]employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]int{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]int, len(list))
	newList := make([]int, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []int{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []int{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeeIntErrNoOrder(f func(employee.Employee) (int, error), list []employee.Employee, worker int) ([]int, error) {
	chJobs := make(chan employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan int, chJobs chan employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]int, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []int{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []int{}, <-errCh
	}

	return newList, nil
}

// PMapEmployeeIntPtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeIntPtr(f func(*employee.Employee) *int, list []*employee.Employee, optional ...fp.Optional) []*int {
	if f == nil {
		return []*int{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeIntPtrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeIntPtrPreserveOrder(f, list, worker)
}

func pMapEmployeeIntPtrPreserveOrder(f func(*employee.Employee) *int, list []*employee.Employee, worker int) []*int {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*int, chJobs chan map[int]*employee.Employee) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*int{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*int, len(list))
	newList := make([]*int, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapEmployeeIntPtrNoOrder(f func(*employee.Employee) *int, list []*employee.Employee, worker int) []*int {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *int, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *int, chJobs chan *employee.Employee) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*int, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapEmployeeIntPtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapEmployeeIntPtrErr(f func(*employee.Employee) (*int, error), list []*employee.Employee, optional ...fp.Optional) ([]*int, error) {
	if f == nil {
		return []*int{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapEmployeeIntPtrErrNoOrder(f, list, worker)
		}
	}

	return pMapEmployeeIntPtrErrPreserveOrder(f, list, worker)
}

func pMapEmployeeIntPtrErrPreserveOrder(f func(*employee.Employee) (*int, error), list []*employee.Employee, worker int) ([]*int, error) {
	chJobs := make(chan map[int]*employee.Employee, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*employee.Employee{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*int, chJobs chan map[int]*employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*int{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*int, len(list))
	newList := make([]*int, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*int{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*int{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapEmployeeIntPtrErrNoOrder(f func(*employee.Employee) (*int, error), list []*employee.Employee, worker int) ([]*int, error) {
	chJobs := make(chan *employee.Employee, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *int, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *int, chJobs chan *employee.Employee, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*int, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*int{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*int{}, <-errCh
	}

	return newList, nil
}

// FilterMapEmployeeInt filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - employee.Employee and returns true/false.
//	2. Function: takes employee.Employee as argument and returns int
// 	3. List of type employee.Employee
//
// Returns:
//	New List of type int
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeInt(fFilter func(employee.Employee) bool, fMap func(employee.Employee) int, list []employee.Employee) []int {
	if fFilter == nil || fMap == nil {
		return []int{}
	}
	var newList []int
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapEmployeeIntPtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *employee.Employee and returns (bool, error).
//	2. Function: takes *employee.Employee as argument and returns (*int, error)
// 	3. List of type *employee.Employee
//
// Returns:
//	New List of type *int, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeIntPtrErr(fFilter func(*employee.Employee) (bool, error), fMap func(*employee.Employee) (*int, error), list []*employee.Employee) ([]*int, error) {
	if fFilter == nil || fMap == nil {
		return []*int{}, nil
	}
	var newList []*int
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapEmployeeIntErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - employee.Employee and returns (bool, error).
//	2. Function: takes employee.Employee as argument and returns (int, error)
// 	3. List of type employee.Employee
//
// Returns:
//	New List of type int, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapEmployeeIntErr(fFilter func(employee.Employee) (bool, error), fMap func(employee.Employee) (int, error), list []employee.Employee) ([]int, error) {
	if fFilter == nil || fMap == nil {
		return []int{}, nil
	}
	var newList []int
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// MapIntEmployer takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapIntEmployer(f func(int) Employer, list []int) []Employer {
	if f == nil {
		return []Employer{}
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapIntEmployerErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapIntEmployerErr(f func(int) (Employer, error), list []int) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}
	newList := make([]Employer, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapIntEmployerPtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapIntEmployerPtr(f func(*int) *Employer, list []*int) []*Employer {
	if f == nil {
		return []*Employer{}
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapIntEmployerPtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapIntEmployerPtrErr(f func(*int) (*Employer, error), list []*int) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}
	newList := make([]*Employer, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapIntEmployer applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployer(f func(int) Employer, list []int, optional ...fp.Optional) []Employer {
	if f == nil {
		return []Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployerNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployerPreserveOrder(f, list, worker)
}

func pMapIntEmployerPreserveOrder(f func(int) Employer, list []int, worker int) []Employer {
	chJobs := make(chan map[int]int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]int) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapIntEmployerNoOrder(f func(int) Employer, list []int, worker int) []Employer {
	chJobs := make(chan int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan int) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapIntEmployerErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployerErr(f func(int) (Employer, error), list []int, optional ...fp.Optional) ([]Employer, error) {
	if f == nil {
		return []Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployerErrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployerErrPreserveOrder(f, list, worker)
}

func pMapIntEmployerErrPreserveOrder(f func(int) (Employer, error), list []int, worker int) ([]Employer, error) {
	chJobs := make(chan map[int]int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]Employer, chJobs chan map[int]int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]Employer, len(list))
	newList := make([]Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapIntEmployerErrNoOrder(f func(int) (Employer, error), list []int, worker int) ([]Employer, error) {
	chJobs := make(chan int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan Employer, chJobs chan int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []Employer{}, <-errCh
	}

	return newList, nil
}

// PMapIntEmployerPtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployerPtr(f func(*int) *Employer, list []*int, optional ...fp.Optional) []*Employer {
	if f == nil {
		return []*Employer{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployerPtrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployerPtrPreserveOrder(f, list, worker)
}

func pMapIntEmployerPtrPreserveOrder(f func(*int) *Employer, list []*int, worker int) []*Employer {
	chJobs := make(chan map[int]*int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*int) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*Employer{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapIntEmployerPtrNoOrder(f func(*int) *Employer, list []*int, worker int) []*Employer {
	chJobs := make(chan *int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *int) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapIntEmployerPtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployerPtrErr(f func(*int) (*Employer, error), list []*int, optional ...fp.Optional) ([]*Employer, error) {
	if f == nil {
		return []*Employer{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployerPtrErrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployerPtrErrPreserveOrder(f, list, worker)
}

func pMapIntEmployerPtrErrPreserveOrder(f func(*int) (*Employer, error), list []*int, worker int) ([]*Employer, error) {
	chJobs := make(chan map[int]*int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*Employer, chJobs chan map[int]*int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*Employer{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*Employer, len(list))
	newList := make([]*Employer, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapIntEmployerPtrErrNoOrder(f func(*int) (*Employer, error), list []*int, worker int) ([]*Employer, error) {
	chJobs := make(chan *int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *Employer, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *Employer, chJobs chan *int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*Employer, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*Employer{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*Employer{}, <-errCh
	}

	return newList, nil
}

// FilterMapIntEmployer filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - int and returns true/false.
//	2. Function: takes int as argument and returns Employer
// 	3. List of type int
//
// Returns:
//	New List of type Employer
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployer(fFilter func(int) bool, fMap func(int) Employer, list []int) []Employer {
	if fFilter == nil || fMap == nil {
		return []Employer{}
	}
	var newList []Employer
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapIntEmployerPtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *int and returns (bool, error).
//	2. Function: takes *int as argument and returns (*Employer, error)
// 	3. List of type *int
//
// Returns:
//	New List of type *Employer, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployerPtrErr(fFilter func(*int) (bool, error), fMap func(*int) (*Employer, error), list []*int) ([]*Employer, error) {
	if fFilter == nil || fMap == nil {
		return []*Employer{}, nil
	}
	var newList []*Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapIntEmployerErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - int and returns (bool, error).
//	2. Function: takes int as argument and returns (Employer, error)
// 	3. List of type int
//
// Returns:
//	New List of type Employer, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployerErr(fFilter func(int) (bool, error), fMap func(int) (Employer, error), list []int) ([]Employer, error) {
	if fFilter == nil || fMap == nil {
		return []Employer{}, nil
	}
	var newList []Employer
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// MapIntEmployee takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapIntEmployee(f func(int) employee.Employee, list []int) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapIntEmployeeErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list and error
func MapIntEmployeeErr(f func(int) (employee.Employee, error), list []int) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}
	newList := make([]employee.Employee, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// MapIntEmployeePtr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list
func MapIntEmployeePtr(f func(*int) *employee.Employee, list []*int) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		newList[i] = f(v)
	}
	return newList
}

// MapIntEmployeePtrErr takes two inputs -
// 1. Function 2. List. Then It returns a new list after applying the function on each item of the list, error
func MapIntEmployeePtrErr(f func(*int) (*employee.Employee, error), list []*int) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}
	newList := make([]*employee.Employee, len(list))
	for i, v := range list {
		r, err := f(v)
		if err != nil {
			return nil, err
		}
		newList[i] = r
	}
	return newList, nil
}

// PMapIntEmployee applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployee(f func(int) employee.Employee, list []int, optional ...fp.Optional) []employee.Employee {
	if f == nil {
		return []employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployeeNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployeePreserveOrder(f, list, worker)
}

func pMapIntEmployeePreserveOrder(f func(int) employee.Employee, list []int, worker int) []employee.Employee {
	chJobs := make(chan map[int]int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]int) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapIntEmployeeNoOrder(f func(int) employee.Employee, list []int, worker int) []employee.Employee {
	chJobs := make(chan int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan int) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapIntEmployeeErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployeeErr(f func(int) (employee.Employee, error), list []int, optional ...fp.Optional) ([]employee.Employee, error) {
	if f == nil {
		return []employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployeeErrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployeeErrPreserveOrder(f, list, worker)
}

func pMapIntEmployeeErrPreserveOrder(f func(int) (employee.Employee, error), list []int, worker int) ([]employee.Employee, error) {
	chJobs := make(chan map[int]int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]employee.Employee, chJobs chan map[int]int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]employee.Employee, len(list))
	newList := make([]employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapIntEmployeeErrNoOrder(f func(int) (employee.Employee, error), list []int, worker int) ([]employee.Employee, error) {
	chJobs := make(chan int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan employee.Employee, chJobs chan int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []employee.Employee{}, <-errCh
	}

	return newList, nil
}

// PMapIntEmployeePtr applies the function(1st argument) on each item in the list and returns a new list.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployeePtr(f func(*int) *employee.Employee, list []*int, optional ...fp.Optional) []*employee.Employee {
	if f == nil {
		return []*employee.Employee{}
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployeePtrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployeePtrPreserveOrder(f, list, worker)
}

func pMapIntEmployeePtrPreserveOrder(f func(*int) *employee.Employee, list []*int, worker int) []*employee.Employee {
	chJobs := make(chan map[int]*int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*int) {
			defer wg.Done()

			for m := range chJobs {
				for k, v := range m {
					chResult <- map[int]*employee.Employee{k: f(v)}
				}
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		for k, v := range m {
			newListMap[k] = v
		}
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList
}

func pMapIntEmployeePtrNoOrder(f func(*int) *employee.Employee, list []*int, worker int) []*employee.Employee {
	chJobs := make(chan *int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *int) {
			defer wg.Done()

			for v := range chJobs {
				chResult <- f(v)
			}
		}(chResult, chJobs)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		newList[i] = v
		i++
	}

	return newList
}

// PMapIntEmployeePtrErr applies the function(1st argument) on each item in the list and returns a new list and error.
//  Order of new list is guaranteed. This feature can be disabled by passing: Optional{RandomOrder: true} to gain performance
//  Run in parallel. no_of_goroutines = no_of_items_in_list or 3rd argument can be passed to fix the number of goroutines.
//
// Takes 3 inputs. 3rd argument is optional
//  1. Function - takes 1 input
//  2. List
//  3. optional argument - fp.Optional{FixedPool: <some_number>}
func PMapIntEmployeePtrErr(f func(*int) (*employee.Employee, error), list []*int, optional ...fp.Optional) ([]*employee.Employee, error) {
	if f == nil {
		return []*employee.Employee{}, nil
	}

	var worker = len(list)
	if optional != nil && len(optional) > 0 {
		if optional[0].FixedPool > 0 && optional[0].FixedPool < worker {
			worker = optional[0].FixedPool
		}

		if optional[0].RandomOrder == true {
			return pMapIntEmployeePtrErrNoOrder(f, list, worker)
		}
	}

	return pMapIntEmployeePtrErrPreserveOrder(f, list, worker)
}

func pMapIntEmployeePtrErrPreserveOrder(f func(*int) (*employee.Employee, error), list []*int, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan map[int]*int, len(list))
	go func() {
		for i, v := range list {
			chJobs <- map[int]*int{i: v}
		}
		close(chJobs)
	}()

	chResult := make(chan map[int]*employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan map[int]*employee.Employee, chJobs chan map[int]*int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for m := range chJobs {
				for k, v := range m {
					r, err := f(v)
					if err != nil {
						errCh <- err
						return
					}
					chResult <- map[int]*employee.Employee{k: r}
				}
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newListMap := make(map[int]*employee.Employee, len(list))
	newList := make([]*employee.Employee, len(list))

	for m := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		for k, v := range m {
			newListMap[k] = v
		}
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	for i := 0; i < len(list); i++ {
		newList[i] = newListMap[i]
	}

	return newList, nil
}

func pMapIntEmployeePtrErrNoOrder(f func(*int) (*employee.Employee, error), list []*int, worker int) ([]*employee.Employee, error) {
	chJobs := make(chan *int, len(list))
	go func() {
		for _, v := range list {
			chJobs <- v
		}
		close(chJobs)
	}()

	chResult := make(chan *employee.Employee, worker/3)

	errCh := make(chan error, len(list))
	var wg sync.WaitGroup

	for i := 0; i < worker; i++ {
		wg.Add(1)

		go func(chResult chan *employee.Employee, chJobs chan *int, errCh chan error) {
			defer wg.Done()
			if len(errCh) > 0 {
				return
			}
			for v := range chJobs {
				r, err := f(v)
				if err != nil {
					errCh <- err
					return
				}
				chResult <- r
			}
		}(chResult, chJobs, errCh)
	}

	// This will wait for the workers to complete their job and then close the channel
	go func() {
		wg.Wait()
		close(chResult)
	}()

	newList := make([]*employee.Employee, len(list))
	i := 0

	for v := range chResult {
		select {
		case err := <-errCh:
			return []*employee.Employee{}, err
		default:
		}
		newList[i] = v
		i++
	}

	if len(errCh) > 0 {
		return []*employee.Employee{}, <-errCh
	}

	return newList, nil
}

// FilterMapIntEmployee filters given list, then apply function(2nd argument) on each item in the list and returns a new list
// Takes 3 inputs
//	1. Function: takes one input type - int and returns true/false.
//	2. Function: takes int as argument and returns employee.Employee
// 	3. List of type int
//
// Returns:
//	New List of type employee.Employee
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployee(fFilter func(int) bool, fMap func(int) employee.Employee, list []int) []employee.Employee {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}
	}
	var newList []employee.Employee
	for _, v := range list {
		if fFilter(v) {
			newList = append(newList, fMap(v))
		}
	}
	return newList
}

// FilterMapIntEmployeePtrErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - *int and returns (bool, error).
//	2. Function: takes *int as argument and returns (*employee.Employee, error)
// 	3. List of type *int
//
// Returns:
//	New List of type *employee.Employee, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployeePtrErr(fFilter func(*int) (bool, error), fMap func(*int) (*employee.Employee, error), list []*int) ([]*employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []*employee.Employee{}, nil
	}
	var newList []*employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}

// FilterMapIntEmployeeErr filters given list, then apply function(2nd argument) on each item in the list and returns a new list, error
// Takes 3 inputs
//	1. Function: takes one input type - int and returns (bool, error).
//	2. Function: takes int as argument and returns (employee.Employee, error)
// 	3. List of type int
//
// Returns:
//	New List of type employee.Employee, error
//  Empty list if all there parameters are nil or either of parameter is nil
func FilterMapIntEmployeeErr(fFilter func(int) (bool, error), fMap func(int) (employee.Employee, error), list []int) ([]employee.Employee, error) {
	if fFilter == nil || fMap == nil {
		return []employee.Employee{}, nil
	}
	var newList []employee.Employee
	for _, v := range list {
		r, err := fFilter(v)
		if err != nil {
			return nil, err
		}
		if r {
			r, err := fMap(v)
			if err != nil {
				return nil, err
			}
			newList = append(newList, r)
		}
	}
	return newList, nil
}


type byIdEmployer []Employer

func (a byIdEmployer) Len() int           { return len(a) }
func (a byIdEmployer) Less(i, j int) bool { return a[i].Id < a[j].Id }
func (a byIdEmployer) Swap(i, j int)      { a[i], a[j] = a[j], a[i] }

// SortEmployerById sort structs
func SortEmployerById(list []Employer) []Employer {
	if len(list) == 0 {
		return []Employer{}
	}
	newList := make([]Employer, len(list))
	for i, item := range list {
		newList[i] = item
	}
	sort.Sort(byIdEmployer(newList))
	return newList
}

// SortEmployerByIdPtr sorts structs
func SortEmployerByIdPtr(list []*Employer) []*Employer {
	if len(list) == 0 {
		return []*Employer{}
	}
	newList := make([]Employer, len(list))
	newListPtr := make([]*Employer, len(list))

	for i, item := range list {
		newList[i] = *item
	}
	sort.Sort(byIdEmployer(newList))

	for i := 0; i < len(newList); i++ {
		newListPtr[i] = &newList[i]
	}
	return newListPtr
}

type byIdEmployerDesc []Employer

func (a byIdEmployerDesc) Len() int           { return len(a) }
func (a byIdEmployerDesc) Less(i, j int) bool { return a[i].Id > a[j].Id }
func (a byIdEmployerDesc) Swap(i, j int)      { a[i], a[j] = a[j], a[i] }

// SortEmployerByIdDesc sorts structs
func SortEmployerByIdDesc(list []Employer) []Employer {
	if len(list) == 0 {
		return []Employer{}
	}
	newList := make([]Employer, len(list))
	for i, item := range list {
		newList[i] = item
	}
	sort.Sort(byIdEmployerDesc(newList))
	return newList
}

// SortEmployerByIdDescPtr sorts structs
func SortEmployerByIdDescPtr(list []*Employer) []*Employer {
	if len(list) == 0 {
		return []*Employer{}
	}
	newList := make([]Employer, len(list))
	newListPtr := make([]*Employer, len(list))

	for i, item := range list {
		newList[i] = *item
	}
	sort.Sort(byIdEmployerDesc(newList))

	for i := 0; i < len(newList); i++ {
		newListPtr[i] = &newList[i]
	}
	return newListPtr
}
// UnionEmployerById return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployerById(arrList ...[]Employer) []Employer {
	resultMap := make(map[int]bool)
	var resultArr []Employer
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[v.Id]
			if !ok {
				resultMap[v.Id] = true
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// Union<FTYPE>Ptr return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployerByIdPtr(arrList ...[]*Employer) []*Employer {
	resultMap := make(map[int]bool)
	var resultArr []*Employer
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[v.Id]
			if !ok {
				resultMap[v.Id] = true
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// IntersectionEmployerById return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployerById(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[int]bool)
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Id == v.Id {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
	}
	return newList
}

// IntersectionEmployerByIdPtr return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployerByIdPtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[int]bool)
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				resultMap[arrList[0][i].Id] = true
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Id == v.Id {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
	}
	return newList
}

// DifferenceEmployerById returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployerById(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[int]bool)
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Id == v.Id {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
	}
	return newList
}

// DifferenceEmployerByIdPtr returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployerByIdPtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[int]bool)
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				resultMap[arrList[0][i].Id] = true
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Id == v.Id {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i].Id]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Id] = true
			}
		}
	}
	return newList
}

// SubsetEmployerById returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployerById(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[int]bool)
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i].Id]
		if !ok {
			found := false
			resultMap[list1[i].Id] = true
			for j := 0; j < len(list2); j++ {
				if list1[i].Id == list2[j].Id {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SubsetEmployerByIdPtr returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployerByIdPtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[int]bool)
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i].Id]
		if !ok {
			found := false
			resultMap[list1[i].Id] = true
			for j := 0; j < len(list2); j++ {
				if list1[i].Id == list2[j].Id {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployerById returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployerById(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[int]bool)

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i].Id]
		if !ok {
			found := false
			resultMap[list2[i].Id] = true
			for j := 0; j < len(list1); j++ {
				if list2[i].Id == list1[j].Id {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployerByIdPtr returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployerByIdPtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[int]bool)

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i].Id]
		if !ok {
			found := false
			resultMap[list2[i].Id] = true
			for j := 0; j < len(list1); j++ {
				if list2[i].Id == list1[j].Id {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SetEmployerById returns a set of the distinct elements of coll.
func SetEmployerById(list []Employer) []Employer {
	if list == nil || len(list) == 0 {
		return []Employer{}
	}

	resultMap := make(map[int]bool)
	newList := []Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i].Id]
		if !ok {
			resultMap[list[i].Id] = true
			newList = append(newList, list[i])
		}
	}
	return newList
}

// SetEmployerByIdPtr returns a set of the distinct elements of coll.
func SetEmployerByIdPtr(list []*Employer) []*Employer {
	if list == nil || len(list) == 0 {
		return []*Employer{}
	}

	resultMap := make(map[int]bool)
	newList := []*Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i].Id]
		if !ok {
			resultMap[list[i].Id] = true
			newList = append(newList, list[i])
		}
	}
	return newList
}
// UnionEmployerByName return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployerByName(arrList ...[]Employer) []Employer {
	resultMap := make(map[string]bool)
	var resultArr []Employer
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[v.Name]
			if !ok {
				resultMap[v.Name] = true
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// Union<FTYPE>Ptr return a set that is the union of the input sets
// repeated value within list parameter will be ignored
func UnionEmployerByNamePtr(arrList ...[]*Employer) []*Employer {
	resultMap := make(map[string]bool)
	var resultArr []*Employer
	for _, arr := range arrList {
		for _, v := range arr {
			_, ok := resultMap[v.Name]
			if !ok {
				resultMap[v.Name] = true
				resultArr = append(resultArr, v)
			}
		}
	}
	return resultArr
}

// IntersectionEmployerByName return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployerByName(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[string]bool)
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Name == v.Name {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
	}
	return newList
}

// IntersectionEmployerByNamePtr return a set that is the intersection of the input sets
// repeated value within list parameter will be ignored
func IntersectionEmployerByNamePtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[string]bool)
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				resultMap[arrList[0][i].Name] = true
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Name == v.Name {
					matchCount++
					break
				}
			}
		}
		if matchCount == len(arrList)-1 {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
	}
	return newList
}

// DifferenceEmployerByName returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployerByName(arrList ...[]Employer) []Employer {
	if arrList == nil {
		return []Employer{}
	}

	resultMap := make(map[string]bool)
	if len(arrList) == 1 {
		var newList []Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
		return newList
	}

	var newList []Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Name == v.Name {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
	}
	return newList
}

// DifferenceEmployerByNamePtr returns a set that is the first set without elements of the remaining sets
// repeated value within list parameter will be ignored
func DifferenceEmployerByNamePtr(arrList ...[]*Employer) []*Employer {
	if arrList == nil {
		return []*Employer{}
	}

	resultMap := make(map[string]bool)
	if len(arrList) == 1 {
		var newList []*Employer
		for i := 0; i < len(arrList[0]); i++ {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				resultMap[arrList[0][i].Name] = true
				newList = append(newList, arrList[0][i])
			}
		}
		return newList
	}

	var newList []*Employer
	// 1st loop iterates items in 1st array
	// 2nd loop iterates all the rest of the arrays
	// 3rd loop iterates items in the rest of the arrays
	for i := 0; i < len(arrList[0]); i++ {

		matchCount := 0
		for j := 1; j < len(arrList); j++ {
			for _, v := range arrList[j] {
				// compare every items in 1st array to every items in the rest of the arrays
				if arrList[0][i].Name == v.Name {
					matchCount++
					break
				}
			}
		}
		if matchCount == 0 {
			_, ok := resultMap[arrList[0][i].Name]
			if !ok {
				newList = append(newList, arrList[0][i])
				resultMap[arrList[0][i].Name] = true
			}
		}
	}
	return newList
}

// SubsetEmployerByName returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployerByName(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[string]bool)
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i].Name]
		if !ok {
			found := false
			resultMap[list1[i].Name] = true
			for j := 0; j < len(list2); j++ {
				if list1[i].Name == list2[j].Name {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SubsetEmployerByNamePtr returns true or false by checking if set1 is a subset of set2
// repeated value within list parameter will be ignored
func SubsetEmployerByNamePtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[string]bool)
	for i := 0; i < len(list1); i++ {
		_, ok := resultMap[list1[i].Name]
		if !ok {
			found := false
			resultMap[list1[i].Name] = true
			for j := 0; j < len(list2); j++ {
				if list1[i].Name == list2[j].Name {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployerByName returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployerByName(list1, list2 []Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[string]bool)

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i].Name]
		if !ok {
			found := false
			resultMap[list2[i].Name] = true
			for j := 0; j < len(list1); j++ {
				if list2[i].Name == list1[j].Name {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SupersetEmployerByNamePtr returns true or false by checking if set1 is a superset of set2
// repeated value within list parameter will be ignored
func SupersetEmployerByNamePtr(list1, list2 []*Employer) bool {
	if list1 == nil || len(list1) == 0 || list2 == nil || len(list2) == 0 {
		return false
	}

	resultMap := make(map[string]bool)

	for i := 0; i < len(list2); i++ {
		_, ok := resultMap[list2[i].Name]
		if !ok {
			found := false
			resultMap[list2[i].Name] = true
			for j := 0; j < len(list1); j++ {
				if list2[i].Name == list1[j].Name {
					found = true
					break
				}
			}
			if !found {
				return false
			}
		}
	}
	return true
}

// SetEmployerByName returns a set of the distinct elements of coll.
func SetEmployerByName(list []Employer) []Employer {
	if list == nil || len(list) == 0 {
		return []Employer{}
	}

	resultMap := make(map[string]bool)
	newList := []Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i].Name]
		if !ok {
			resultMap[list[i].Name] = true
			newList = append(newList, list[i])
		}
	}
	return newList
}

// SetEmployerByNamePtr returns a set of the distinct elements of coll.
func SetEmployerByNamePtr(list []*Employer) []*Employer {
	if list == nil || len(list) == 0 {
		return []*Employer{}
	}

	resultMap := make(map[string]bool)
	newList := []*Employer{}
	for i := 0; i < len(list); i++ {
		_, ok := resultMap[list[i].Name]
		if !ok {
			resultMap[list[i].Name] = true
			newList = append(newList, list[i])
		}
	}
	return newList
}